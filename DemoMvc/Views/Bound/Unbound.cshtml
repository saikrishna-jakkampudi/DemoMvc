
@{
    ViewBag.Title = "Unbound";
}

<h2>Unbound</h2>

@using (Html.BeginForm("Showdata","Bound"))
{

<table>
    <tr>
        <td>
            Empno
        </td>
        <td>
            @Html.TextBox("txtEmpno")
        </td>
    </tr>
    <tr>
        <td> Ename</td>
        <td>
            @Html.TextBox("txtEname")
        </td>
    </tr>

    <tr>
        <td> Salary</td>
        <td>
            @Html.TextBox("txtSalary")
        </td>
    </tr>
    <tr>
        <td><input id="Submit1" type="submit" value="Submit" /> </td>
    </tr>
</table>
}